# Deny direct web access to dotfiles/dotdirs such as .git and .env.
# DirectoryMatch is not allowed inside .htaccess on shared Apache/Plesk.
<IfModule mod_rewrite.c>
    RewriteEngine On
    RewriteRule (^|/)\. - [F,L]
</IfModule>
